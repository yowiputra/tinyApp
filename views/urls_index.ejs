<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <header>
    <% include ./partials/_header %>
  </header>

  <main>
      <ul>
        <% for (var id in urls){ %>
          <li>
            <a href= http://localhost:8080/urls/<%= id%>><%= id%></a>: <%= urls[id]%><br/>
          </li>
        <% } %>
      </ul>
  </main>

</body>
</html>
